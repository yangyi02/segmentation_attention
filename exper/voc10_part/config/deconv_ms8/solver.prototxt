train_net: "${EXP}/config/${NET_ID}/train_${TRAIN_SET}.prototxt"

lr_policy: "step"
gamma: 0.1
stepsize: 50000
base_lr: 0.00001

average_loss: 100
display: 100
max_iter: 100000
momentum: 0.99
weight_decay: 0

snapshot: 50000
snapshot_prefix: "${EXP}/model/${NET_ID}/train"
solver_mode: GPU
