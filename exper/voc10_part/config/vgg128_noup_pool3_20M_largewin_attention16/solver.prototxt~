train_net: "${EXP}/config/${NET_ID}/train_${TRAIN_SET}.prototxt"

iter_size: 6
lr_policy: "step"
gamma: 0.1
stepsize: 400
base_lr: 0.001

average_loss: 10
display: 10
max_iter: 1200
momentum: 0.9
weight_decay: 0.0005

snapshot: 400
snapshot_prefix: "${EXP}/model/${NET_ID}/train"
solver_mode: GPU
